packages:
  # KMP chess rules engine — built by Gradle, linked as @chesstopia/chess-engine
  - 'chess-engine'
  # React/Vite frontend — consumes @chesstopia/chess-engine via workspace link
  - 'chesstopia-frontend'

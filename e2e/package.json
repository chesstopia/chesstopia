{
  "name": "e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "test": "playwright test --project=chromium --project=mechanik",
    "smoke": "playwright test smoke.spec.ts --project=chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0"
  }
}

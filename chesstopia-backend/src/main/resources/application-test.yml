spring:
  test:
    database:
      replace: any
  jpa:
    hibernate:
      ddl-auto: validate

# Run the embedded Postgres from native binaries instead of Docker (no daemon required).
zonky:
  test:
    database:
      provider: zonky

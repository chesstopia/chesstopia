---
# Install with: ansible-galaxy collection install -r requirements.yml
collections:
  - name: community.general   # ufw
  - name: ansible.posix       # sysctl, authorized_key
  - name: community.docker    # docker_container (registry)

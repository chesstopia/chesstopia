# Managed by Ansible (chesstopia deploy_target role) - do not edit by hand.
# Consumed by docker-compose.prod.yml on `docker compose up -d`.
{% for key, value in app_env.items() %}
{{ key }}={{ value }}
{% endfor %}

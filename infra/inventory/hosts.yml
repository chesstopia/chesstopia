---
all:
  vars:
    # Default connection user is the admin account. For the first contact with a
    # fresh box, override on the CLI: -e ansible_user=root
    ansible_user: "{{ admin_user }}"
    ansible_python_interpreter: /usr/bin/python3
  children:
    prod:
      hosts:
        prod-app:
          ansible_host: 46.225.235.229
          prod_private_ip: 10.0.0.2   # Hetzner private network
    infra:
      hosts:
        infra-build:
          ansible_host: 167.233.134.101
          infra_private_ip: 10.0.0.3 # Hetzner private network
